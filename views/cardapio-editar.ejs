<!DOCTYPE html>
<html lang="pt-br">
<head>
  <%- include('partials/head', { title: 'Editar item' }) %>
</head>
<body class="bg-stone-100 min-h-screen font-sans text-stone-800">

<div class="flex">
  <%- include('partials/sidebar', { active: 'cardapio' }) %>

  <main class="flex-1 p-8">
    <div class="max-w-2xl mx-auto">
      <h1 class="text-3xl font-bold text-stone-900 mb-2">Editar item</h1>
      <p class="text-stone-500 mb-8">Altere os dados do item do cardápio.</p>

      <div class="bg-white rounded-2xl shadow-sm border border-stone-200/80 p-6">
        <form method="POST" action="/admin/cardapio/editar/<%= item.id %>" class="space-y-4">
          <div>
            <label class="block text-sm font-medium text-stone-600 mb-1.5">Nome</label>
            <input name="nome" value="<%= item.nome %>" required class="w-full border border-stone-300 rounded-xl px-4 py-2.5 focus:ring-2 focus:ring-amber-500/30 focus:border-amber-500">
          </div>
          <div>
            <label class="block text-sm font-medium text-stone-600 mb-1.5">Preço (R$)</label>
            <input name="preco" type="number" step="0.01" min="0" value="<%= item.preco %>" required class="w-full border border-stone-300 rounded-xl px-4 py-2.5 focus:ring-2 focus:ring-amber-500/30 focus:border-amber-500">
          </div>
          <div>
            <label class="block text-sm font-medium text-stone-600 mb-1.5">Descrição (opcional)</label>
            <input name="descricao" value="<%= item.descricao || '' %>" class="w-full border border-stone-300 rounded-xl px-4 py-2.5 focus:ring-2 focus:ring-amber-500/30 focus:border-amber-500">
          </div>
          <div>
            <label class="block text-sm font-medium text-stone-600 mb-1.5">Categoria</label>
            <select name="categoria" class="w-full border border-stone-300 rounded-xl px-4 py-2.5 focus:ring-2 focus:ring-amber-500/30 focus:border-amber-500">
              <option value="prato" <%= item.categoria === 'prato' || !item.categoria ? 'selected' : '' %>>Prato</option>
              <option value="bebida" <%= item.categoria === 'bebida' ? 'selected' : '' %>>Bebida</option>
            </select>
          </div>
          <div>
            <label class="flex items-center gap-2 cursor-pointer">
              <input name="ativo" type="checkbox" <%= item.ativo ? 'checked' : '' %> class="rounded border-stone-300 text-amber-600 focus:ring-amber-500">
              <span class="text-sm text-stone-600">Ativo (aparece no cardápio do agente)</span>
            </label>
          </div>
          <div class="flex gap-3 pt-2">
            <button type="submit" class="bg-amber-600 hover:bg-amber-700 text-white font-medium px-6 py-2.5 rounded-xl transition shadow-sm">Salvar</button>
            <a href="/admin/cardapio" class="bg-stone-200 hover:bg-stone-300 text-stone-800 font-medium px-6 py-2.5 rounded-xl transition">Cancelar</a>
          </div>
        </form>
      </div>
    </div>
  </main>
</div>

</body>
</html>
